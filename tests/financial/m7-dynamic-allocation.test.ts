import { describe,expect,it } from "vitest";import { validateDynamicAllocationPlan,DYNAMIC_ALLOCATION_POLICY_VERSION,DYNAMIC_ALLOCATION_PROFILE_VERSION } from "@/domain/allocation/dynamic-plan";
const a={admissionId:"a",candidateId:"c",assetIdentity:{identityVersion:"asset-identity/v1" as const,assetClass:"SYNTHETIC",namespace:"x",canonicalIdentifier:"x",providerExternalIds:[]},asOf:"2026-01-01T00:00:00.000Z",admissionPolicyVersion:"universe-admission-policy/v1" as const,eligibilityEvaluationId:"e",eligibilityPolicyVersion:"asset-eligibility-policy/v1",eligibilityProfileVersion:"mm-synthetic-eligibility-profile/v1",status:"ADMITTED" as const,reasonCodes:[],provenance:[],configFingerprint:"f"};const base={planVersion:DYNAMIC_ALLOCATION_POLICY_VERSION,profileVersion:DYNAMIC_ALLOCATION_PROFILE_VERSION,asOf:new Date("2026-01-01"),admissionDecisions:[a],dynamicTargets:[{assetIdentity:"x",admissionId:"a",targetBps:300n}],datasetPins:["d"]} as const;
describe("M7 allocation plans",()=>{it("scales base weights and reconciles 10000 bps",()=>{const r=validateDynamicAllocationPlan(base);expect(r.status).toBe("VALID");expect(Object.values(r.completeTargetVector).reduce((n,x)=>n+BigInt(x),0n)).toBe(10000n);expect(r.completeTargetVector.x).toBe("300");});it("rejects non-admitted and capped targets",()=>expect(validateDynamicAllocationPlan({...base,dynamicTargets:[{...base.dynamicTargets[0]!,targetBps:501n}],admissionDecisions:[{...a,status:"REJECTED"}]}).status).toBe("INVALID"));it("is order independent and preserves zero-dynamic legacy vector",()=>{const r=validateDynamicAllocationPlan({...base,dynamicTargets:[]});expect(r.baseTargets).toEqual({"mm.fixture.global.v1":"6000","mm.fixture.growth.v1":"2500","mm.fixture.defensive.v1":"1500"});expect(r.configHash).toBe(validateDynamicAllocationPlan({...base,dynamicTargets:[]}).configHash);});});
