import { describe,expect,it } from "vitest";
import { decideUniverseAdmission } from "@/domain/universe/admission";
import { projectAdminActivity } from "@/domain/activity/admin-activity";
const identity={identityVersion:"asset-identity/v1" as const,assetClass:"SYNTHETIC",namespace:"fixture",canonicalIdentifier:"asset-x",providerExternalIds:["p"]};
const evaluation={evaluationId:"e",candidateId:"c",asOf:"2026-01-01T00:00:00.000Z",policyVersion:"asset-eligibility-policy/v1" as const,profileVersion:"mm-synthetic-eligibility-profile/v1" as const,status:"ELIGIBLE" as const,checks:[],fingerprint:"f"};
describe("M6 admission and activity",()=>{it("admits only supported eligible candidates deterministically",()=>{const a=decideUniverseAdmission(identity,"c",evaluation,new Date("2026-01-02"));expect(a.status).toBe("ADMITTED");expect(a).toEqual(decideUniverseAdmission(identity,"c",evaluation,new Date("2026-01-02")));});it("rejects incomplete or invalid eligibility",()=>{expect(decideUniverseAdmission(identity,"c",{...evaluation,status:"INCOMPLETE"},new Date("2026-01-02")).status).toBe("INCOMPLETE");expect(decideUniverseAdmission({...identity,assetClass:"UNKNOWN"},"c",evaluation,new Date("2026-01-02")).status).toBe("REJECTED");});it("projects deterministic filtered paginated activity",()=>{const item={id:"2",occurredAt:"2026-01-02T00:00:00Z",category:"UNIVERSE_ADMISSION" as const,severity:"PASS" as const,title:"Admission",summary:"Admitted",policyVersions:["universe-admission-policy/v1"],datasetVersions:[],reasonCodes:["OK"],provenanceRefs:[],details:{}};expect(projectAdminActivity([item],{category:"UNIVERSE_ADMISSION",limit:1})).toEqual([item]);});});
