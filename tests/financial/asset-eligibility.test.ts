import { describe, expect, it } from "vitest";
import { evaluateAssetEligibility } from "@/domain/discovery/asset-eligibility";
describe("M5 deterministic eligibility",()=>{const base={candidateId:"c",assetClass:"SYNTHETIC",ageDays:30n,historySpanDays:14n,distinctEligibleVenueCount:2n,liquidityMinor:1000000n,volumeMinor:500000n,marketCapMinor:10000000n,top10HolderConcentrationBps:8000n,singleHolderConcentrationBps:3000n,volatilityBps:20000n,evidenceIds:["e"]};it("passes the synthetic profile deterministically",()=>{const a=evaluateAssetEligibility(base,new Date("2026-01-01"));expect(a.status).toBe("ELIGIBLE");expect(a).toEqual(evaluateAssetEligibility(base,new Date("2026-01-01")));});it("fails closed for missing evidence",()=>expect(evaluateAssetEligibility({...base,liquidityMinor:undefined},new Date("2026-01-01")).status).toBe("INCOMPLETE"));it("rejects explicit violations and suspicious flags",()=>expect(evaluateAssetEligibility({...base,volatilityBps:20001n,suspiciousFlags:["SUSPICIOUS_ACTIVITY"]},new Date("2026-01-01")).status).toBe("INELIGIBLE"));it("does not treat trend context as eligibility authority",()=>expect(evaluateAssetEligibility({...base,trendContext:"RISK_ON"} as never,new Date("2026-01-01")).status).toBe("ELIGIBLE"));});
